
zuul:
  #ignoredServices: '*'
  routes:
    gestususervice:
      path: /gestususervice/**
      serviceId: GESTUSUSERVICE
      stripPrefix: false
      #url: http://gestususervice:9999/api/

# Discovery Server Access
eureka:
  client:
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://serverdiscovery:8761/eureka/


server:
  port: 8080   # HTTP (Tomcat) port
