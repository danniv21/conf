zuul:
  #ignoredServices: '*'
  routes:
    salesorderapi:
      path: /salesorderapi/**
      serviceId: SALESORDERAPI 
# Discovery Server Access
eureka:
  client:
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://serverdiscovery:8761/eureka/


server:
  port: 8097   # HTTP  port
  
management.security.enabled=false

